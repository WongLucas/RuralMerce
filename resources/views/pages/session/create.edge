@layout.app({title: Register})
  @slot('main')
    @form({action: route('login.store')})
      <div class="flex justify-center">
        <div class="flex flex-col">
          <div>
            <label>Email:</label>
            <input type="email" name="email" class="border p-1 rounded-2xl">
          </div>
          <div>
            <label>Senha:</label>
            <input type="password" name="password" class="border p-1 rounded-2xl">
          </div>
          <button type="submit">CRIAR</button>
        </div>
      </div>
    @end
  @end
@end
