@layout.app()
  @slot('main')
    @form({ action:route('item.store') })
      <div>
        <label>Nome: </label>
        <input type="text" name="name" class="border">
      </div>

      <div>
        <label>Descrição: </label>
        <textarea name="description" cols="30" rows="10" class="border"></textarea>
      </div>

      <div>
        <label>Categorias:</label>
        <div>
          @each(category in categories)
            <label >
              <input
              type="checkbox"
              name="categories[]"
              value="{{ category.id }}"
              >
              {{ category.name }}
            </label>
          @end
        </div>
      </div>

      <button type="submit">Criar</button>
    @end
  @end
@end
