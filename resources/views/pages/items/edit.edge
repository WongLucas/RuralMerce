<!DOCTYPE html>
<html>
<head>
  <title>Items</title>
  <meta charset="UTF-8">
</head>
<body>
  <div>Editar</div>
  @form({action: route('item.update', { id: item.id }, { qs: { _method: 'PUT' }})})
    <input type="text" name="name" value="{{ item.name }}">
    <input type="text" name="description" value="{{ item.description }}">
    <button type="submit">editar</button>
  @end
</body>
</html>
